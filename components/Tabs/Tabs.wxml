<view class="tabs">
  <view class="tabs_title">
    <view class="title_item {{item.isActive?'active':''}}" wx:for="{{tabs}}" bindtap="handleItemTap" data-index="{{index}}" wx:key="id">
      {{item.value}}
    </view>
  </view>
  <view class="tabs_content">
    <slot></slot>
  </view>
</view>